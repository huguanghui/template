# perl 速查手册

## 简介

> perl 是一种强大的脚本语言, 主要用于脚本处理, 代码生成. 代码简洁高效.
  难学易忘.

## 语法规则

创建变量

标量, 列表, 哈希

标量
```
$var = "str01";
$num = 123;

print "$var\n";
print "$num\n";
```

流控制

子程序

钻石操作符

特殊字符

use函数

q, qw, qr, qx, qq 用法

q - 相当于'', 可以使用()[]{}//,,
qx - 引号执行运算符, 相当于``
qq - 相当于"", 转义字符有效, 可以使用()[]{}//,,
qw - 相当于(",", '')在每个单词上添加'', 转义字符无效, 可以使用()[]{}//,,
qr - 相当于创建正则

> 编译时引入

## 使用细节
