5. 终极排查建议
如果 Prefix 失效时，你发现：
输入任何字符都没有反应 -> 大概率是 Ctrl-s 冻结，按 Ctrl-q 解决。
输入的字符变成了带背景色的特殊显示 -> 确定是 Bracketed Paste Mode 没闭合，输入 printf "\e[201~" 解决。
Prefix 失效但回车能换行 -> 检查是否进入了 Tmux 的 copy-mode（右上角是否有数字），按 q 退出。
推荐做法：
在 .zshrc 中添加 stty -ixon，并在粘贴长路径时，尽量先在编辑器里整理好，或者使用 Ctrl-v (在 Zsh 中) 绕过某些插件的解析。
