# 输出模板

复制以下骨架，替换 `<>` 占位符。保持章节顺序与标题编号。

````markdown
# `<target-dir>` 模块代码分析报告

> 分析对象：`<target-dir>`（<一句话定位>）
> 编译入口：`<CMakeLists 中的目标/变量>`；进程名 `<proc>`
> 外部依赖：<SDK/库/框架>

---

## 一、目录与架构说明

### 1.1 目录树

```text
<tree 风格，每行末尾 # 功能简述>
```

### 1.2 分层架构

<分层表格：层 | 文件 | 职责>

<svg>...整体架构 / 数据流图...</svg>

### 1.3 启动与生命周期挂载点

<谁在何处调用 init/deinit；触发条件；重入/自愈机制>

---

## 二、核心数据结构

### 2.1 <核心上下文结构>

<表格：字段 | 含义>

### 2.2 <会话/状态结构>

```c
<关键 struct 定义>
```

### 2.3 <协议/帧/外部契约结构>

<表格或代码块>

---

## 三、主干逻辑流程图

### 3.1 <初始化/去初始化链路>

<svg>...流程/回滚图...</svg>

### 3.2 <主干业务控制面>

<svg>...主干流程图（必选，含判断分支）...</svg>

### 3.3 <数据面/时序>

<svg>...泳道/回环图...</svg>

---

## 四、关键函数解析

### 4.1 `<函数名>()`（`<文件>`）

<职责、关键逻辑、边界条件、风险点>

（按调用链顺序逐个函数）

---

## 五、可优化建议

### P0 · 正确性与并发安全

1. **<问题标题>** —— <现象 + 位置>；建议 <动作>。

### P1 · 资源与架构

<n. ...>

### P2 · 代码质量与清理

<n. ...>

---

## 附：模块调用关系速查

| 入口 | 直接调用 | 最终落点 |
| --- | --- | --- |
| ... | ... | ... |
````
